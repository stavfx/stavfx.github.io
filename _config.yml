title: Stav Raviv
description: Software Engineer at Eluvio — projects, background, and links.
repository: stavfx/stavfx.github.io

# Custom static site (index.html + assets/css). No Jekyll theme by design.
# Exclude local tooling from the build output.
exclude:
  - Gemfile
  - Gemfile.lock
  - Makefile
  - vendor
